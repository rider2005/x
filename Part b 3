Write a C/C++ program to implement the ln/rename command using general file API’s. 

#include <stdio.h>
#include<unistd.h> provides access to the link() function used to create a hard link.
int main(int argc, char *argv[])
{
if(argc!=3)
{
printf(“usage: %s <src_file><dest_file>\n”,argv[0]);
Return 0;
}
if(link(argv[1],argv[2])==-1)
{
printf(“link error\n”);
return 1;
}
printf(“files linked\n”);
printf(“Inode number of linked files\n”);
char str[100];
sprintf(str,”ls –i %s %s \n”,argv[1],argv[2]);  sprint is short and fixed time frame during which a specific set of task takes place.
creates a command string to list inode numbers for both files.

system(str);  
runs the command created in str using the system shell, displaying each file’s inode information.
return 0;
}
